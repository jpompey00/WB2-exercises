"use strict"
//area of rectangle is l*w

var length = 2;
var width = 6;


let result  = `The area of the rectangle is ${length * width}`

//result with string concatenation
console.log(
    "The area of the rectangle is " + length * width
)

//result written with string literals
console.log(result)
